# Supported platforms

Community Preview targets 64-bit Linux on amd64 and arm64 with a current Docker Engine and Compose v2. Recommended minimums are 4 GB RAM, two CPU cores, and 10 GB free disk plus media capacity. Rootless Docker and orchestrators other than Compose are not yet qualified. macOS and Windows may be useful for evaluation but are not supported deployment targets.
